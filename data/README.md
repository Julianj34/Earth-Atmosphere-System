# Data

This folder contains generated data files used by the Earth Atmosphere System.

The data is separated into:

- `states/` – latest JSON state files for each layer
- `history/` – long-term snapshot history used for system analysis

The data files are outputs of the layer notebooks and form the basis for later research and hypothesis testing.
